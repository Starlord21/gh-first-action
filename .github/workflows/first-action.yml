name: First Workflow
on : workflow_dispatch
jobs:
  first-job :
    runs-on: ubuntu-latest
    steps:
      - name: Print gretting
      - run: echo "Hello Word"
      - name: Print Done
      - run : echo "Done Bye"
